# Variáveis de ambiente do AgroVisita
# Nenhuma chave ou token é necessária nesta versão.
# Se no futuro o app precisar de API keys backend ou serviços de mapas, adicione-as aqui
# e NUNCA commite o arquivo .env real (ele está no .gitignore).

AGROVISTA_ENV=development